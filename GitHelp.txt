Команды для регистрации ваших данных
1) git config -- global user.name "your_username"
2) git config -- global user.email "your_username@gmail.com"

Команды только для одного проекта
3) git init        |Инициализация Гита
4) git remote add origin link_to_your_repository  |Добавление репозитория

5) git status       | Статус проекта
6) git add .        | Сохранение
6) git add . или путь к файлу

7) git commit -m "lesson1_m2 added".   |Описание изменений/ Сохранение- добавление
7) git commit -m "update GitHelp.txt"  |Сохранение - обновление

8) git push origin master |Отправка изменения/ Сохранение

9) git restore. |или путь к файлу
9) git reset. |или путь к файлу

10) git branch | покажет все ветки
11) git branch name_branch
12) git switch name_branch
13) git clone ссылка на проект


production
test|stage
dev

feat/register
feat/booking

"feat: added register code"
"update: update register code"
"fix: delete bag"
"HotFix: delete bag"

# Для каждого проекта создается отдельное виртуальное окружение
# подключение venv:
py -m venv name_venv
python -m venv name_venv   | python -m venv my_venv     # для macBook
python3 -m venv name_venv

# Включение виртуального окружения:
source name_venv/bin/activate                          # для macBook
name_venv\Scripts\activate
source name_venv\Scripts\activate

# Скачивание внешнего модуля, библиотеки:
pip install

# Примеры:
pip install requests      # HTTP-запросы
pip install faker         # Фейковые данные
pip install colorama      # Цветной текст в терминале
pip install random2       # Случайные числа